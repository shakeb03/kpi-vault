# Prompt — Event spine for two fictional titles

**Goal:** Synthetic cross-title events with versioned schema stubs.

**Hard rules:**
- Titles must be fictional only (Atlas Siege, Nova Lane)
- Do not use real studio IP names, store products, or live economics
- Event types: install, session_start, purchase, plus title-flavored play events
- Enough volume for DAU spark + D1 cohort math

**Ask:** Seed strategy + ingest endpoint that invalidates Redis dashboard keys.
