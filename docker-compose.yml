services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bi
      POSTGRES_PASSWORD: bi_dev_only
      POSTGRES_DB: definition_safe_bi
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./server/sql/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bi -d definition_safe_bi"]
      interval: 3s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 20

  api:
    build: ./server
    ports:
      - "3848:3848"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: "3848"
      # Reach data stores via published host ports (works when inter-container
      # bridge forwarding is restricted; also works on normal Docker Desktop).
      DATABASE_URL: postgres://bi:bi_dev_only@host.docker.internal:5433/definition_safe_bi
      REDIS_URL: redis://host.docker.internal:6380
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  web:
    build: ./client
    ports:
      - "3847:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - api

volumes:
  pgdata:
